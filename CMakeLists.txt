# vim: ts=4:sw=4:expandtab
CMAKE_MINIMUM_REQUIRED(VERSION 3.0)

PROJECT(bstool LANGUAGES C)
ADD_EXECUTABLE(${PROJECT_NAME} bstool.c bsdiff.c bspatch.c)

IF(MSVC)
    ADD_COMPILE_DEFINITIONS(_CRT_SECURE_NO_WARNINGS)
ENDIF()